#include "main.h"
/**
 * add - says it will just add two integer... i hope!
 * @x: subject one
 * @y: subject two
 * Return: result of addition
 */
int add(int x, int y)
{
	int result;

	result = x + y;

	return (result);
}
