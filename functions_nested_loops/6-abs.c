#include <stdlib.h>
#include "main.h"
/**
 * _abs - will compute the absolute value of an integer
 * @c: will become positive if negative
 *
 * Return: 0 (Success)
 */
int _abs(int c)
{
	if (c < 0)
		c = (-c);
	else
		c = c;
	return (0);
}
